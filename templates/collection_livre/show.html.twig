{% extends 'base.html.twig' %}

{% block title %}CollectionLivre{% endblock %}

{% block body %}
    <h1>CollectionLivre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection_livre.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_collection_livre_index') }}">back to list</a>

    <a href="{{ path('app_collection_livre_edit', {'id': collection_livre.id}) }}">edit</a>

    {{ include('collection_livre/_delete_form.html.twig') }}
{% endblock %}
