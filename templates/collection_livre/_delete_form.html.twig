<form method="post" action="{{ path('app_collection_livre_delete', {'id': collection_livre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ collection_livre.id) }}">
    <button class="btn">Delete</button>
</form>
